apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zitadel-chart.fullname" . }}-test-connection"
  labels:
    {{- include "zitadel-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zitadel-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

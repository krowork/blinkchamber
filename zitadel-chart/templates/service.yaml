apiVersion: v1
kind: Service
metadata:
  name: {{ include "zitadel-chart.fullname" . }}
  labels:
    {{- include "zitadel-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zitadel-chart.selectorLabels" . | nindent 4 }}
